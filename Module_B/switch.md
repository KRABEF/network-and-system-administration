# Конфигурация коммутатора на Alt linux

## 1. Установить имя машины:
```bash
hostnamectl set-hostname название_машины
# например
hostnamectl set-hostname SW1-HQ
```
перезапустить машину для применения нового имени.

